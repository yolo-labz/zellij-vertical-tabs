/target
/result
/result-*
*.wasm
.direnv

# AI tooling config — keep local, never commit
CLAUDE.md
CLAUDE.local.md
AGENTS.md
/.claude/
/.cursor/
.aider*
/.specify/
.github/copilot-instructions.md

# cargo-fuzz (CI generates its own lock; corpora are ephemeral)
fuzz/target/
fuzz/corpus/
fuzz/artifacts/
fuzz/coverage/
fuzz/Cargo.lock
